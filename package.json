{
  "dependencies": {
    "concurrently": "^7.2.1",
    "dom-parser": "^0.1.6",
    "fs-extra": "^10.1.0",
    "glob": "^8.0.1",
    "showdown": "^2.1.0",
    "showdown-highlight": "^3.0.0"
  },
  "scripts": {
    "build": "node bssg.js",
    "dev": "node bssg.js",
    "preserver": "npm run build",
    "server": "serve public",
    "watch": "nodemon -e '*' --watch ./src --watch ./layout --exec node bssg.js"
  }
}
